# LiveKit Agents SDK
livekit-agents>=0.8.0
livekit-plugins-openai>=0.6.0
livekit-plugins-silero>=0.6.0

# Optional STT/TTS providers (install as needed)
livekit-plugins-deepgram>=0.6.0  # For Deepgram Nova-3, Flux STT and Aura TTS
livekit-plugins-google>=0.6.0    # For Google Cloud Speech-to-Text

# Environment and Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Document Processing
python-docx>=1.0.0

# Data Export and Analysis
pandas>=2.0.0

# LiveKit API (for join_survey.py)
livekit-api>=0.5.0

# Zoom Bridge Integration (Phase 1: Recall.ai)
fastapi>=0.104.0           # Webhook server
uvicorn[standard]>=0.24.0  # ASGI server
requests>=2.31.0           # HTTP client for Recall.ai API
numpy>=1.24.0              # Audio data processing
pydub>=0.25.0              # Audio format conversion (PCM to MP3)
                           # Note: Also requires ffmpeg to be installed on system
                           # Install with: brew install ffmpeg (macOS)
